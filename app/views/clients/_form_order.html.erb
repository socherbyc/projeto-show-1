<%= form_with(model: order, local: true, url: { action: :orders_create }) do |form| %>
    <% if order.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>
  
        <ul>
        <% order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :number %>
      <%= form.text_field :number, id: :order_number %>
    </div>

    <div class="field">
      <%= form.label :products_ids_amount %>
      id=amount,id=amount,id=amount,...<br>
      <%= form.text_field :products_ids_amount, id: :products_ids_amount %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>
