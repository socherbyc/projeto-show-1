<%= form_with(model: order_detail, url: url, local: true) do |form| %>
  <% if order_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_detail.errors.count, "error") %> prohibited this order_detail from being saved:</h2>

      <ul>
      <% order_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, id: :order_detail_amount %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :order_detail_price %>
  </div>

  <div class="field">
    <%= form.label :order_number %>
    <%= form.text_field :order_number, id: :order_detail_order_number %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :order_detail_product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
